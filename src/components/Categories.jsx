import React from 'react';
import SectionHeader from './SectionHeader';

const Categories = () => {
    return (
        <div>
            <SectionHeader subHeader={'from 11:00PM to 10:00PM'} header={'Order Online'}></SectionHeader>
        </div>
    );
};

export default Categories;